# Optional overrides for resource-service.
# Where the latest portal.html is fetched from (defaults to this repo's main).
# PORTAL_URL=https://raw.githubusercontent.com/Jdoink/sovereignty_stack/main/command-center-portal/portal.html
# Where the future Resource Vault JSON lives inside the container.
# RESOURCE_DATA_PATH=/data
